import time, os

fn = input('first number: ')
sn = input('second number: ')
mid = input('* / + - : ')

if mid == '*':
	print((fn)*(sn))

if mid == '/':
	print(mid)

if mid == '+':
	print(mid)

if mid == '-':
	print(mid)

os.system('pause')
